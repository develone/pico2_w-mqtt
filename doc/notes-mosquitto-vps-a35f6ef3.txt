11/07/25
diff-conf-file
ubuntu@vps-a35f6ef3:~$ less m-pw
testuser:$7$101$tAn1/qhCFyqnw0Sm$7x0BZC8K1XqGzEv1WhyAb3GiRMKivrDqQ9Y0BtbimirnKT94j0WffYC6yPEt0GpAupW3LOlS3KyB/+zlO/kBCg==

ubuntu@vps-a35f6ef3:~$ ./scripts-rpi/mosquitto/mos.sh 
1762557628: mosquitto version 2.0.18 starting
1762557628: Config loaded from /etc/mosquitto/mosquitto.conf.
1762557628: Warning: File /home/ubuntu/m-pw has world readable permissions. Future versions will refuse to load this file.
To fix this, use `chmod 0700 /home/ubuntu/m-pw`.
1762557628: Opening ipv4 listen socket on port 1883.
1762557628: Opening ipv6 listen socket on port 1883.
1762557628: mosquitto version 2.0.18 running
ubuntu@vps-a35f6ef3:~$ 1762557637: New connection from 172.59.41.99:38445 on port 1883.
1762557637: New client connected from 172.59.41.99:38445 as remote3 (p2, c1, k10, u'testuser').
1762557658: Client remote3 has exceeded timeout, disconnecting.
1762557662: New connection from 172.59.41.99:35154 on port 1883.
1762557662: New client connected from 172.59.41.99:35154 as remote3 (p2, c1, k10, u'testuser').
1762557669: New connection from 172.59.41.99:24847 on port 1883.
1762557669: Client remote3 already connected, closing old connection.
1762557669: New client connected from 172.59.41.99:24847 as remote3 (p2, c1, k10, u'testuser').
1762557688: Client remote3 has exceeded timeout, disconnecting.
1762557688: New connection from 172.59.41.99:65483 on port 1883.
1762557688: New client connected from 172.59.41.99:65483 as remote3 (p2, c1, k10, u'testuser').
1762557706: Client remote3 has exceeded timeout, disconnecting.
1762557707: New connection from 172.59.41.99:11262 on port 1883.
1762557707: New client connected from 172.59.41.99:11262 as remote3 (p2, c1, k10, u'testuser').


buntu@vps-a35f6ef3:~$ ./scripts-rpi/mosquitto/mos-q.sh 
T = 34.63 C 0x03d batt = 0.05 remote3 
T = 34.63 C 0x03d batt = 0.05 remote3
diff --git a/pico_w/wifi/freertos/iperf/picow_freertos_iperf.c b/pico_w/wifi/freertos/iperf/picow_freertos_iperf.c
index 8070ceb..dc41f24 100644
--- a/pico_w/wifi/freertos/iperf/picow_freertos_iperf.c
+++ b/pico_w/wifi/freertos/iperf/picow_freertos_iperf.c
@@ -2,7 +2,7 @@
  * Copyright (c) 2022 Raspberry Pi (Trading) Ltd.
  *
  * SPDX-License-Identifier: BSD-3-Clause
-06/21/25
+06/30/25
  */
 #include "tcp_debug.h"
 #include "head_tail.h"
@@ -13,6 +13,7 @@
 #include "pico/stdlib.h"
 
 #include <string.h>
+
 #include "lwip/dns.h"
 #include "lwip/pbuf.h"
 #include "lwip/udp.h"
@@ -35,7 +36,7 @@ int rr[6];
 #define CLOSE_TASK_PRIORITY				( tskIDLE_PRIORITY + 10UL )
 #define OPEN_TASK_PRIORITY				( tskIDLE_PRIORITY + 9UL )
 #define ADC_TASK_PRIORITY				( tskIDLE_PRIORITY + 8UL )
-//#define NTP_TASK_PRIORITY				( tskIDLE_PRIORITY + 5UL )
+#define NTP_TASK_PRIORITY				( tskIDLE_PRIORITY + 5UL )
 #define WATCHDOG_TASK_PRIORITY			( tskIDLE_PRIORITY + 1UL )
 #define MQTT_TASK_PRIORITY				( tskIDLE_PRIORITY + 4UL )  
 #define SOCKET_TASK_PRIORITY			( tskIDLE_PRIORITY + 6UL )
@@ -221,8 +222,8 @@ static void alarm_callback(void) {
 
 			/*192.168.1.212 0xc0a801d4 LWIP_MQTT_EXAMPLE_IPADDR_INIT pi4-50*/
 			//#define LWIP_MQTT_EXAMPLE_IPADDR_INIT = IPADDR4_INIT(PP_HTONL(0xc0a801d4))
-			/*192.168.1.253 0xc0a801fd LWIP_MQTT_EXAMPLE_IPADDR_INIT pi5-90*/
-			#define LWIP_MQTT_EXAMPLE_IPADDR_INIT = IPADDR4_INIT(PP_HTONL(0xc0a801fd))
+			/*40.160.235.122 0x28a0eb7a LWIP_MQTT_EXAMPLE_IPADDR_INIT ubunt-x86-64*/
+			#define LWIP_MQTT_EXAMPLE_IPADDR_INIT = IPADDR4_INIT(PP_HTONL(0x28a0eb7a))
 
 	#else
 			#define LWIP_MQTT_EXAMPLE_IPADDR_INIT
@@ -691,7 +692,7 @@ void open_task(__unused void *params) {
 
 
 /*needed for ntp*/
-/*
+
 void ntp_task(__unused void *params) {
     //bool on = false;
     //printf("ntp_task starts\n");
@@ -710,8 +711,7 @@ void ntp_task(__unused void *params) {
         vTaskDelay(2200);
     }
 }
-*/
-
+/*needed for ntp*/
 
 void adc_task(__unused void *params) {
     //bool on = false;
@@ -878,7 +878,7 @@ void main_task(__unused void *params) {
         //printf("failed to initialise\n");
         return;
     }
-	watchdog_enable(10000, 1);
+	watchdog_enable(12500, 1);
 	//while (wifi_connected) {
     	cyw43_arch_enable_sta_mode();
     	printf("Connecting to Wi-Fi...\n");
@@ -947,7 +947,7 @@ void init_pico_mqtt(void) {
     xTaskCreate(socket_task, "SOCKETThread", configMINIMAL_STACK_SIZE, NULL, SOCKET_TASK_PRIORITY, NULL);
 	xTaskCreate(mqtt_task, "MQTTThread", configMINIMAL_STACK_SIZE, NULL, MQTT_TASK_PRIORITY, NULL);
     //xTaskCreate(rtc_task, "RTCThread", configMINIMAL_STACK_SIZE, NULL, RTC_TASK_PRIORITY, NULL);
-    //xTaskCreate(ntp_task, "NTPThread", configMINIMAL_STACK_SIZE, NULL, NTP_TASK_PRIORITY, NULL);
+    xTaskCreate(ntp_task, "NTPThread", configMINIMAL_STACK_SIZE, NULL, NTP_TASK_PRIORITY, NULL);
     //xTaskCreate(gpio_task, "GPIOThread", configMINIMAL_STACK_SIZE, NULL, GPIO_TASK_PRIORITY, NULL);
     xTaskCreate(adc_task, "ADCThread", configMINIMAL_STACK_SIZE, NULL, ADC_TASK_PRIORITY, NULL);
     /*setting default temperature units*/
@@ -1000,6 +1000,27 @@ sprintf(client_message,"01234567890123456789012345678901234567890123456789012345
 6789012345678901234567890123456789012301234567890123456789012345678901234567890123456789012345678901\n");
  
 }
+
+/*needed for ntp*/
+// Called with results of operation
+static void ntp_result(NTP_T* state, int status, time_t *result) {
+    if (status == 0 && result) {
+        struct tm *utc = gmtime(result);
+        printf("got ntp response: %02d/%02d/%04d %02d:%02d:%02d\n", utc->tm_mday, utc->tm_mon + 1, utc->tm_year + 1900,
+               utc->tm_hour, utc->tm_min, utc->tm_sec);
+        sprintf(tmp, " %02d/%02d/%04d %02d:%02d:%02d\n", utc->tm_mday, utc->tm_mon + 1, utc->tm_year + 1900,
+               utc->tm_hour, utc->tm_min, utc->tm_sec);
+        ptrhead = head_tail_helper(ptrhead, ptrtail, ptrendofbuf, ptrtopofbuf, tmp);
+
+    }
+
+    if (state->ntp_resend_alarm > 0) {
+        cancel_alarm(state->ntp_resend_alarm);
+        state->ntp_resend_alarm = 0;
+    }
+    state->ntp_test_time = make_timeout_time_ms(NTP_TEST_TIME);
+    state->dns_request_sent = false;
+}              
 /*
 void set_rtc(datetime_t *pt, datetime_t *pt_ntp,datetime_t *palarm) {
 	if(rtc_set_flag==0) {
@@ -1031,9 +1052,133 @@ void set_rtc(datetime_t *pt, datetime_t *pt_ntp,datetime_t *palarm) {
         //printf("0x%x 0x%x\n",&t,&alarm);
         //printf("pt 0x%x palarm 0x%x\n",pt,palarm);
 }
-*/
+/*needed for ntp*/
+// Called with results of operation
+
 
 
+static int64_t ntp_failed_handler(alarm_id_t id, void *user_data);
+
+// Make an NTP request
+static void ntp_request(NTP_T *state) {
+    // cyw43_arch_lwip_begin/end should be used around calls into lwIP to ensure correct locking.
+    // You can omit them if you are in a callback from lwIP. Note that when using pico_cyw_arch_poll
+    // these calls are a no-op and can be omitted, but it is a good practice to use them in
+    // case you switch the cyw43_arch type later.
+    cyw43_arch_lwip_begin();
+    struct pbuf *p = pbuf_alloc(PBUF_TRANSPORT, NTP_MSG_LEN, PBUF_RAM);
+    uint8_t *req = (uint8_t *) p->payload;
+    memset(req, 0, NTP_MSG_LEN);
+    req[0] = 0x1b;
+    udp_sendto(state->ntp_pcb, p, &state->ntp_server_address, NTP_PORT);
+    pbuf_free(p);
+    cyw43_arch_lwip_end();
+}
+
+static int64_t ntp_failed_handler(alarm_id_t id, void *user_data)
+{
+    NTP_T* state = (NTP_T*)user_data;
+    printf("ntp request failed\n");
+    ntp_result(state, -1, NULL);
+	wifi_connected=1;
+    return 0;
+}
+
+// Call back with a DNS result
+static void ntp_dns_found(const char *hostname, const ip_addr_t *ipaddr, void *arg) {
+    NTP_T *state = (NTP_T*)arg;
+    if (ipaddr) {
+        state->ntp_server_address = *ipaddr;
+        printf("ntp address %s\n", ip4addr_ntoa(ipaddr));
+        ntp_request(state);
+    } else {
+       printf("ntp dns request failed\n");
+        ntp_result(state, -1, NULL);
+    }
+}
+
+// NTP data received
+static void ntp_recv(void *arg, struct udp_pcb *pcb, struct pbuf *p, const ip_addr_t *addr, u16_t port) {
+    NTP_T *state = (NTP_T*)arg;
+    uint8_t mode = pbuf_get_at(p, 0) & 0x7;
+    uint8_t stratum = pbuf_get_at(p, 1);
+
+    // Check the result
+    if (ip_addr_cmp(addr, &state->ntp_server_address) && port == NTP_PORT && p->tot_len == NTP_MSG_LEN &&
+        mode == 0x4 && stratum != 0) {
+        uint8_t seconds_buf[4] = {0};
+        pbuf_copy_partial(p, seconds_buf, sizeof(seconds_buf), 40);
+        uint32_t seconds_since_1900 = seconds_buf[0] << 24 | seconds_buf[1] << 16 | seconds_buf[2] << 8 | seconds_buf[3];
+        uint32_t seconds_since_1970 = seconds_since_1900 - NTP_DELTA;
+        time_t epoch = seconds_since_1970;
+        ntp_result(state, 0, &epoch);
+    } else {
+        printf("invalid ntp response\n");
+        ntp_result(state, -1, NULL);
+    }
+    pbuf_free(p);
+}
+
+// Perform initialisation
+static NTP_T* ntp_init(void) {
+    NTP_T *state = calloc(1, sizeof(NTP_T));
+    if (!state) {
+        printf("failed to allocate state\n");
+        return NULL;
+    }
+    state->ntp_pcb = udp_new_ip_type(IPADDR_TYPE_ANY);
+    if (!state->ntp_pcb) {
+        printf("failed to create pcb\n");
+        free(state);
+        return NULL;
+    }
+    udp_recv(state->ntp_pcb, ntp_recv, state);
+    return state;
+}
+
+// Runs ntp test forever
+void run_ntp_test(void) {
+    NTP_T *state = ntp_init();
+    if (!state)
+        return;
+    while(true) {
+        if (absolute_time_diff_us(get_absolute_time(), state->ntp_test_time) < 0 && !state->dns_request_sent) {
+
+            // Set alarm in case udp requests are lost
+            state->ntp_resend_alarm = add_alarm_in_ms(NTP_RESEND_TIME, ntp_failed_handler, state, true);
+
+            // cyw43_arch_lwip_begin/end should be used around calls into lwIP to ensure correct locking.
+            // You can omit them if you are in a callback from lwIP. Note that when using pico_cyw_arch_poll
+            // these calls are a no-op and can be omitted, but it is a good practice to use them in
+            // case you switch the cyw43_arch type later.
+            cyw43_arch_lwip_begin();
+            int err = dns_gethostbyname(NTP_SERVER, &state->ntp_server_address, ntp_dns_found, state);
+            cyw43_arch_lwip_end();
+
+            state->dns_request_sent = true;
+            if (err == ERR_OK) {
+                ntp_request(state); // Cached result
+            } else if (err != ERR_INPROGRESS) { // ERR_INPROGRESS means expect a callback
+                printf("dns request failed\n");
+                ntp_result(state, -1, NULL);
+            }
+        }
+#if PICO_CYW43_ARCH_POLL
+        // if you are using pico_cyw43_arch_poll, then you must poll periodically from your
+        // main loop (not from a timer) to check for WiFi driver or lwIP work that needs to be done.
+        cyw43_arch_poll();
+        sleep_ms(1);
+#else
+        // if you are not using pico_cyw43_arch_poll, then WiFI driver and lwIP work
+        // is done via interrupt in the background. This sleep is just an example of some (blocking)
+        // work you might be doing.
+        sleep_ms(1000);
+#endif
+    }
+    free(state);
+}
+/*needed for ntp*/
+
 
 int main( void )
 {

devel@pi5-90:~ $ cat scripts-rpi/mosquitto/mos-q.sh 
#!/bin/bash
mosquitto_sub -t 'pico/status' -u 'testuser' -P 'password123'

devel@pi5-90:~ $ ping -c 1 vps-a35f6ef3 
PING vps-a35f6ef3 (40.160.235.122) 56(84) bytes of data.
64 bytes from vps-a35f6ef3 (40.160.235.122): icmp_seq=1 ttl=45 time=75.5 ms

--- vps-a35f6ef3 ping statistics ---
1 packets transmitted, 1 received, 0% packet loss, time 0ms
rtt min/avg/max/mdev = 75.546/75.546/75.546/0.000 ms
devel@pi5-90:~ $ mosquitto_sub -h 'vps-a35f6ef3' -t 'pico/status' -u 'testuser' -P 'password123'
T = 30.88 C 0x03d batt = 0.05 remote3 
T = 30.88 C 0x03d batt = 0.05 remote3

Nmap scan report for remote3.lan (192.168.12.134)
Host is up (0.13s latency)

Nmap scan report for remote4.lan (192.168.12.233)
Host is up (0.13s latency).


ubuntu@vps-a35f6ef3:~$ 1762557637: New connection from 172.59.41.99:38445 on po>
1762557637: New client connected from 172.59.41.99:38445 as remote3 (p2, c1, k1>
